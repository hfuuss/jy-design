{
  "name": "root",
  "private": true,
  "scripts": {
    "bs": "lerna bootstrap",
    "ls": "lerna ls",
    "update": "lerna update",
    "clean": "lerna clean",
    "publish": "cd packages/jyui && npm run build && lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.13.0"
  }
}
